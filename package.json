{
  "name": "solairew-financeiro",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html app.js styles.css dist/ && cp -R assets dist/assets",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name solairew-financeiro"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
